spring:
  application:
    name: classcustomerschool
  profiles:
    active: development

---

  spring:
    profiles: development
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: configurationschool