spring:
  application:
    name: securityschool
  profiles:
    active: development

  cloud:
    config:
      discovery:
        enabled: true
        serviceId: configurationschool

security:
  oauth2:
    resource:
      filter-order: 5